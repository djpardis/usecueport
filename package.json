{
  "name": "usecueport",
  "version": "0.0.1",
  "private": true,
  "description": "Marketing site for Cueport, the DJ desktop and mobile app.",
  "scripts": {
    "start": "eleventy --serve --port=8090",
    "build": "eleventy",
    "clean": "rm -rf _site",
    "release": "node scripts/release.mjs",
    "download:link": "node scripts/generate-download-link.mjs",
    "email:access": "node scripts/render-access-email.mjs",
    "email:send-access": "node scripts/send-access-email.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
